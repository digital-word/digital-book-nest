specVersion: "v1"
serviceId: "digital-book-fbaa0-service"
location: "us-east4"
schema:
    source: "./schema"
    datasource:
      postgresql:
        database: "digital-book-fbaa0-database"
        cloudSql:
          instanceId: "digital-book-fbaa0-instance"
          # schemaValidation: "STRICT"     # STRICT mode makes Postgres schema match Data Connect exactly.
          # schemaValidation: "COMPATIBLE" # COMPATIBLE mode makes Postgres schema compatible with Data Connect.
connectorDirs: ["./notes"]